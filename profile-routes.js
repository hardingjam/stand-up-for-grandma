// including profile routes here,
const express = require("express");
const router = express.Router();

// router is now an object that has all the express routring functionality.
// basically an ALIAS of "app" in our server.js

exports.router = router;
